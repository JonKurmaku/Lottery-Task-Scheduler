This repository contains a custom implementation of a Lottery Task Scheduler integrated directly into the Linux kernel. It introduces a proportional-share scheduling algorithm where processes compete for CPU time probabilistically based on assigned tickets. The project demonstrates low-level systems programming by modifying the core kernel scheduling framework and includes automated benchmarking scripts (utilizing hackbench and lmbench) to validate correctness and evaluate latency trade-offs.
